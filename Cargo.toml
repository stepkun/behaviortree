# Copyright © Stephan Kunz
[package]
resolver = "3"
name = "behaviortree"
version = "0.0.0"
authors = ["stepkun <stephan.kunz@kabelbw.de>"]
description = "A BehaviorTreelibrary"
edition = "2024"
rust-version = "1.86.0"
license-file = "LICENSE"
repository = "https://github.com/stepkun/behaviortree.git"
homepage = "http://github.com/stepkun/behaviortree.git"
documentation = "https://docs.rs/behaviortree/latest/behaviortree/"
readme = "README.md"
exclude = [
    "benches/**",
    "examples/**",
    "tests/**",
]

[workspace.lints.rust]
unsafe_code = "warn"
missing_docs = "warn"
#unused="allow" # for dev only

[workspace.lints.rustdoc]
private_intra_doc_links = "allow"

[workspace.lints.clippy]
enum_glob_use = "deny"
pedantic = { level = "deny", priority = -1 }
nursery = { level = "deny", priority = -1 }
unwrap_used = "deny"

[lib]
name = "behaviortree"

[lints]
workspace = true

[dependencies]
behaviortree-derive = { path = "derive", version = "0.0.0"}

[dev-dependencies]
#criterion = "0.7.0"
#rstest = "0.26.0"
trybuild = { version = "1.0.106", features = ["diff"] }

[features]
default = ["std"]
std = []

[profile.release]
opt-level = 's'			# Optimize for size.
lto = true				# Enable Link Time Optimisation
codegen-units = 1		# Reduced to increase optimisations
overflow-checks = true	# useful

[profile.production]
inherits="release"
panic = 'abort'		    # Abort on panic
strip = "symbols"	    # Strip symbols from binary

[profile.samply]
inherits = "release"
debug = true		    # Create debug symbols for profiling
